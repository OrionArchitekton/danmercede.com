# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
build
*.local

# CI-vendored substrate clone (substrate-sync workflow + ci.yml fail-open checkout)
vendor/

# Compile status marker (written by scripts/compileContent.ts main(); read by
# CI to branch WROTE vs SKIPPED verification gates — see AGENTS.md).
.compile-status

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
